SELECT
  CONCAT(mpan,' - ',meter_point_id) AS record_id,
  METER_ID,
  METER_POINT_ID,
  MARKET,
  MPXN AS mpan,
  ENERGISED_FLAG,
  PRODUCT_END_DATE,
  AGREEMENT_START_DATE,
  METERPOINT_SUPPLY_START_DATE,
  PROFILE_CLASS,
  ESTIMATED_CONSUMPTION_TOTAL,
  METER_TYPE,
  SMART_ELIGIBILITY,
  SMART_METER_INDICATOR,
  SMART_COMPLIANT_INDICATOR,
  SMART_DESCRIPTION
FROM flk_dub_db_datalake_prd.prod_output_kraken_cnp.cnp_portfolio
WHERE market = 'SME'
AND fuel = 'Elec';